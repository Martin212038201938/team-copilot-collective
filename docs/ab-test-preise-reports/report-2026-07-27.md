# A/B-Test „Preise auszeichnen" – Wochenreport

**Datum:** 2026-07-27 · **Zeitraum:** letzte 7 Tage (Test läuft seit 2026-07-21) · **Status:** ⚠️ Keine Live-Daten abrufbar

## Kurzfazit

Microsoft Clarity war in diesem automatisierten Lauf **nicht zugänglich** – das Browser-Profil ist nicht bei Clarity angemeldet (Login erfordert manuelle Microsoft-Anmeldung, kein aktives Session-Cookie vorhanden). Es konnten daher **keine echten Zahlen** abgerufen werden. Es wurden keine Werte geschätzt oder erfunden.

## Kennzahlen A vs. B

| Variante | Sessions (n) | Leads* | Conversion-Rate |
|---|---|---|---|
| A – ohne Preis | — nicht abrufbar | — nicht abrufbar | — nicht abrufbar |
| B – mit Preis-Störer | — nicht abrufbar | — nicht abrufbar | — nicht abrufbar |

\* Leads = `booking_click` + `contact_form_submit` + `konfigurator_submit` (zusätzlich zu beobachten: `danke_page_view`, `mail_click`).

**Empfehlung:** Keine Aussage möglich – Datenabruf fehlgeschlagen.

## Was Martin manuell in Clarity prüfen sollte

1. In Clarity (Projekt copilotenschule.de) einloggen → Filter auf Custom-Tag `ab_pricing` setzen, Segmente **A** und **B** getrennt betrachten (Zeitraum: letzte 7 Tage sowie kumuliert seit 21.07.2026).
2. Je Segment die **Sessions-Anzahl (n)** ablesen.
3. Je Segment die Conversion-Events zählen: `booking_click`, `contact_form_submit`, `konfigurator_submit` (= Leads), sowie ergänzend `danke_page_view` und `mail_click`.
4. Conversion-Rate berechnen = Leads / Sessions je Variante, Differenz in Prozentpunkten bilden.
5. Optional: Tag `ab_pricing_product` filtern, um die Verteilung auf die 4 Testtrainings zu sehen (copilot-grundlagen-prompt-design, microsoft-365-copilot-praxis, train-the-trainer-copilot, copilot-studio-ki-agenten).

**Faustregel Aussagekraft:** Bei < ~100 Sessions oder < ~10 Conversions je Variante ist das Ergebnis noch nicht belastbar.

## Hinweis zum Vorlauf

Laut Report vom 21.07.2026 lag zu Testbeginn noch kein `ab_pricing`-Tag in den Clarity-Daten vor (Test war erst wenige Stunden live). Bis heute sind 6 Tage vergangen – die Tags sollten inzwischen befüllt sein, sobald ein manueller Login erfolgt.

## Nächster Report prüft

1. Segment `ab_pricing = A` vs. `= B` → Sessions je Variante (7 Tage + kumuliert seit 21.07.).
2. Je Segment: `booking_click`, `contact_form_submit`, `konfigurator_submit`, `danke_page_view`, `mail_click`.
3. Conversion-Rate = Leads / Sessions, Differenz in Prozentpunkten.
4. `ab_pricing_product` → Beitrag der 4 Testtrainings.

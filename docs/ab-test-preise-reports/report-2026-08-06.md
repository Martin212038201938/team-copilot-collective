# A/B-Test „Preise auszeichnen" – Wochenreport

**Datum:** 2026-08-06 · **Zeitraum:** letzte 7 Tage (Test läuft seit 2026-07-21, Tag 16) · **Status:** 🟡 Daten vorhanden, aber 0 Conversions in beiden Varianten

## Kurzfazit

Clarity war in diesem Lauf zugänglich (Login durch Martin hergestellt). Für den Zeitraum der letzten 7 Tage liegen die Segmente `ab_pricing = A` und `ab_pricing = B` vor. Beide Varianten haben in diesem Zeitraum **0 Leads** erzeugt – weder `booking_click`, `contact_form_submit`, `konfigurator_submit` noch `danke_page_view` wurden ausgelöst. Das Tag `mail_click` existiert aktuell **nicht** in Clarity (nicht in der Tag-Liste vorhanden – vermutlich nie implementiert oder anders benannt).

## Kennzahlen A vs. B (letzte 7 Tage)

| Variante | Sessions (n) | Leads* | Conversion-Rate |
|---|---|---|---|
| A – ohne Preis | 21 | 0 | 0% |
| B – mit Preis-Störer | 17 | 0 | 0% |

\* Leads = `booking_click` + `contact_form_submit` + `konfigurator_submit`. Einzelwerte je Variante: `booking_click` = 0/0, `contact_form_submit` = 0/0, `konfigurator_submit` = 0/0 (A/B). Ergänzend geprüft: `danke_page_view` = 0/0 (A/B).

**Führende Variante:** keine – beide bei 0%, kein Unterschied feststellbar. **Aussagekraft:** nicht gegeben (n = 21 bzw. 17 Sessions, deutlich unter der Faustregel von ~100; 0 Conversions statt ~10). **Empfehlung:** ⏳ **NOCH ZU FRÜH** – zusätzlich auffällig, dass in 7 Tagen mit insgesamt 38 Sessions über beide Varianten hinweg keinerlei Lead-Event ausgelöst wurde.

## Auffälligkeit – bitte prüfen

0 Conversions bei 38 Sessions in 7 Tagen ist ungewöhnlich wenig, selbst für eine frühe Testphase. Zwei mögliche Ursachen:
1. Echtes Nutzerverhalten (Trainingsseiten-Besucher konvertieren aktuell einfach selten – ggf. saisonal/Ferien-bedingt, siehe frühere Tiefenanalyse vom 05.08.).
2. Tracking-Lücke: Die Conversion-Tags feuern möglicherweise nicht zuverlässig auf den 4 Testtrainings-Seiten. Empfehlung: einmal manuell eine Test-Buchung/Kontaktanfrage auf einer der Testseiten durchklicken und prüfen, ob das Event in Clarity ankommt.

## Offene Punkte für den nächsten Lauf

1. **Kumulierter Wert seit Teststart (21.07.):** In diesem Lauf nicht abgerufen – der Datumsfilter in Clarity ließ sich über die Automatisierung nicht zuverlässig auf einen längeren Zeitraum umstellen (UI-Interaktionsproblem, kein Datenproblem). Bitte im nächsten Report erneut versuchen oder manuell in Clarity mit „Benutzerdefiniert" 21.07.–Datum prüfen.
2. **`ab_pricing_product`-Verteilung:** aus Zeitgründen in diesem Lauf nicht geprüft. Zeigt, welches der 4 Testtrainings (copilot-grundlagen-prompt-design, microsoft-365-copilot-praxis, train-the-trainer-copilot, copilot-studio-ki-agenten) den Traffic/die Sessions treibt.
3. **`mail_click`:** Tag existiert nicht in Clarity – falls dieses Event gewünscht ist, muss es erst im Code ergänzt werden.

## Nächster Report prüft

1. Segment `ab_pricing = A` vs. `= B` → Sessions je Variante (7 Tage + kumuliert seit 21.07., inkl. Ursachen-Check für die 0-Conversion-Auffälligkeit).
2. Je Segment: `booking_click`, `contact_form_submit`, `konfigurator_submit`, `danke_page_view`.
3. Conversion-Rate = Leads / Sessions, Differenz in Prozentpunkten.
4. `ab_pricing_product` → Beitrag der 4 Testtrainings.

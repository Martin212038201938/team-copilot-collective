# A/B-Test „Preise auszeichnen" – Wochenreport

**Datum:** 2026-07-21 · **Zeitraum:** letzte 7 Tage · **Status:** 🟢 Test seit heute LIVE – noch keine Messdaten

## Kurzfazit
Der Test ist **heute um ca. 17:30 Uhr live gegangen** (Commit „A/B Preise", f40539a, gepusht 17:21; Deployment per Bundle-Check auf copilotenschule.de bestätigt – `ab_pricing` ist im Live-JavaScript enthalten). In Microsoft Clarity (Projekt Copilotenschule, letzte 7 Tage) existiert das Custom-Tag `ab_pricing` **noch nicht** – erwartbar, da seit Go-Live erst wenige Stunden vergangen sind und Clarity-Tags mit Verzögerung auflaufen. Es liegen daher **keine A/B-Zahlen** vor; es wurden keine Werte geschätzt oder erfunden.

## Kennzahlen A vs. B

| Variante | Sessions (n) | Leads* | Conversion-Rate |
|---|---|---|---|
| A – ohne Preis | — | — | — |
| B – mit Preis-Störer | — | — | — |

\* Leads = `booking_click` + `contact_form_submit` + `konfigurator_submit` (zusätzlich beobachtet: `danke_page_view`, `mail_click`).

**Kontext:** Gesamttraffic der Website: **637 Sessions** in den letzten 7 Tagen (Clarity, 55 Bot-Sitzungen ausgeschlossen). Da nur Besucher der 4 Testtrainings-Seiten einer Variante zugewiesen werden, füllt sich die Stichprobe langsamer als der Gesamttraffic vermuten lässt.

**Führende Variante:** — · **Aussagekraft:** keine Daten (Test lief 0 volle Tage) · **Empfehlung:** ⏳ **NOCH ZU FRÜH** – erste bewertbare Zahlen frühestens im nächsten Wochenreport; Entscheidung erst ab ≥ ~100 Sessions bzw. ≥ ~10 Conversions je Variante.

## Geprüft in diesem Lauf
1. Clarity-Dashboard, letzte 7 Tage: Filter → Benutzerdefinierte Filter → Tag-Liste enthält aktuell: `campaign_*`, `danke_page_view`, `lead`, `pdf_download`, `sml_landing_page_visit`, `visitor_type` – **kein `ab_pricing`**, noch keine der neuen Conversion-Tags (`booking_click` etc.). Diese sind im Live-Code vorhanden und laufen mit den ersten echten Sessions auf.
2. Live-Site: B-Route `/trainings/preis/copilot-grundlagen-prompt-design` antwortet mit HTTP 200 (SPA-Fallback, nicht pre-rendered – für den Test unkritisch und verhindert eine Indexierung der Preis-Variante; Variante/Preis-Störer werden client-seitig gerendert).

## Nächster Report prüft
1. Segment `ab_pricing = A` vs. `= B` → Sessions je Variante (7 Tage + kumuliert seit 21.07.).
2. Je Segment: `booking_click`, `contact_form_submit`, `konfigurator_submit`, `danke_page_view`, `mail_click`.
3. Conversion-Rate = Leads / Sessions, Differenz in Prozentpunkten.
4. `ab_pricing_product` → Beitrag der 4 Testtrainings (`copilot-grundlagen-prompt-design`, `microsoft-365-copilot-praxis`, `train-the-trainer-copilot`, `copilot-studio-ki-agenten`).

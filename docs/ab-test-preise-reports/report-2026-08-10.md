# A/B-Test „Preise auszeichnen" – Wochenreport

**Datum:** 2026-08-10 · **Zeitraum:** letzte 7 Tage (Test läuft seit 2026-07-21, Tag 20) · **Status:** 🔴 Keine Live-Daten abrufbar

## Kurzfazit

Microsoft Clarity war in diesem Lauf **nicht erreichbar**: Die Claude-in-Chrome-Extension ist nicht verbunden (zwei Verbindungsversuche fehlgeschlagen), daher konnte kein Login und kein Segment-Abruf erfolgen. Es liegen für diesen Report **keine aktuellen Zahlen** vor – bewusst wurden keine Werte geschätzt oder fortgeschrieben.

| Variante | Sessions (n) | Leads* | Conversion-Rate |
|---|---|---|---|
| A – ohne Preis | keine Daten | keine Daten | – |
| B – mit Preis-Störer | keine Daten | keine Daten | – |

\* Leads = `booking_click` + `contact_form_submit` + `konfigurator_submit`

**Führende Variante:** nicht bestimmbar. **Empfehlung:** ⏳ **NOCH ZU FRÜH** – unverändert zum Stand 06.08. (damals 21 vs. 17 Sessions, beide 0 Conversions); ohne neue Daten keine Entscheidungsgrundlage.

## Was Martin manuell in Clarity prüfen sollte

clarity.microsoft.com → Projekt copilotenschule.de → Filter auf Custom Tag `ab_pricing`, je einmal Wert **A** und Wert **B**, Zeitraum letzte 7 Tage:

1. **Sessions je Variante** (Stichprobengröße n) – A vs. B.
2. **Lead-Events je Variante:** `booking_click`, `contact_form_submit`, `konfigurator_submit`.
3. **Ergänzend:** `danke_page_view`. (`mail_click` existiert laut Prüfung vom 06.08. nicht in Clarity und muss ggf. erst im Code ergänzt werden.)
4. **`ab_pricing_product`** – Verteilung über die 4 Testtrainings (copilot-grundlagen-prompt-design, microsoft-365-copilot-praxis, train-the-trainer-copilot, copilot-studio-ki-agenten).

## Technischer Blocker

Damit der automatische Wochenreport wieder Zahlen liefert, muss die Claude-in-Chrome-Extension installiert und im Chrome-Seitenpanel mit demselben Konto angemeldet sein wie die Desktop-App. Solange das nicht der Fall ist, kann der geplante Task nur diesen Platzhalter-Report erzeugen.

## Weiterhin offen (aus Report 06.08.)

- Kumulierter Wert seit Teststart 21.07. (Datumsfilter in Clarity über Automatisierung bisher nicht zuverlässig umstellbar).
- Ursachen-Check für 0 Conversions bei 38 Sessions: echtes Nutzerverhalten (Ferien) vs. Tracking-Lücke. Empfehlung unverändert: einmal manuell eine Buchung/Kontaktanfrage auf einer Testseite durchklicken und prüfen, ob das Event in Clarity ankommt.
- `mail_click` ist nicht implementiert.

# OpenAI API Configuration
# 🔒 WICHTIG: Kopiere diese Datei nach .env.local und füge deinen echten API-Key ein
# ⚠️  NIEMALS .env.local zu Git committen! (steht in .gitignore)

# OpenAI API-Key (von https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-proj-YOUR-API-KEY-HERE

# OpenAI Model-Konfiguration
OPENAI_MODEL=gpt-4o
OPENAI_MAX_TOKENS=24000
OPENAI_TEMPERATURE=0.6

# E-Mail Configuration
# Die Website nutzt PHP mail() auf AlwaysData mit automatischem SMTP
# Stelle sicher, dass noreply@copilotenschule.de als Email-Account existiert
# oder copilotenschule.de als autorisierte Sender-Domain konfiguriert ist

# WICHTIG FÜR SETUP:
# 1. Kopiere diese Datei: cp .env.example .env.local
# 2. Trage deinen echten API-Key in .env.local ein
# 3. .env.local wird automatisch von scripts/generate-content.js geladen
# 4. Siehe OPENAI_KEY_SETUP.md für Details

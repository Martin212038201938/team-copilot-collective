# OpenAI API Configuration
# Copy this file to .env.local and add your actual API key
# NEVER commit .env.local or .env files to git!

VITE_OPENAI_API_KEY=your-api-key-here

# SMTP Configuration for sending emails from contact forms via AlwaysData
# These are used by Netlify Functions in production
# Get your SMTP credentials from your AlwaysData account

# SMTP host - usually smtp-[username].alwaysdata.net
SMTP_HOST=smtp-username.alwaysdata.net

# SMTP port - use 587 for TLS (recommended) or 465 for SSL
SMTP_PORT=587

# SMTP username - your AlwaysData account email or username
SMTP_USER=your-username@alwaysdata.net

# SMTP password - your AlwaysData account password or app-specific password
SMTP_PASS=your-smtp-password

# From address for emails (optional, defaults to noreply@copilotenschule.de)
SMTP_FROM=noreply@copilotenschule.de

# OpenAI API Configuration
# Copy this file to .env.local and add your actual API key
# NEVER commit .env.local or .env files to git!

VITE_OPENAI_API_KEY=your-api-key-here

# E-Mail Configuration
# The website uses PHP mail() function on AlwaysData which automatically
# uses the AlwaysData SMTP server - no additional configuration needed!
# Just ensure that noreply@copilotenschule.de exists as an email account
# in AlwaysData, or configure copilotenschule.de as an authorized sender domain.
